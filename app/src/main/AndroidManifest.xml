<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.ardafirdausr.reado">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TextScannerActivity" />
        <activity android:name=".WordActivity" />
        <activity android:name=".AlphabetActivity" />
        <activity android:name=".FunQuizActivity" />
        <activity android:name=".Level1Activity" />
        <activity android:name=".Level2Activity" />
        <activity android:name=".LevelOne" />
        <activity android:name=".LevelTwo" />
        <activity android:name=".LevelThree" />
        <activity android:name=".Level3Activity" />
        <activity android:name=".LevelEnd"></activity>
    </application>

</manifest>