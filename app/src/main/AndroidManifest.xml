<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="me.ardafirdausr.reado">

    // SEMUA YANG DAPAT DI LIHAT DI SETTING HP
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".TextScannerActivity" />
        <activity android:name=".WordActivity" />
        <activity android:name=".AlphabetActivity" />
        <activity android:name=".StageActivity" />
        <activity android:name=".LevelActivity" />
        <activity android:name=".QuizStageOneActivity" />
        <activity android:name=".QuizStageTwoActivity" />
        <activity android:name=".QuizStageThreeActivity"></activity>
    </application>

</manifest>